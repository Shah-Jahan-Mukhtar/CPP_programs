#include<iostream>
using namespace std;
int main ()
{
	int year,salary,bonus,grade;
	cout<<"enter the value salary"<<endl;
	cin>>salary;
	cout<<"enter the value of grade"<<endl;
	cin>>grade;
	cout<<"enter the value of year"<<endl;
	cin>>year;

	cout<<"bonus=" <<salary*50/100;
cout<<"bonus="<<salary*25/100;
salary=salary+bonus;
	if (year>2018)
	if (grade>16)
	cout<<"if year is greater than 2018 then bounes will be declared "<<endl;
else
cout<<"if year is smaller than 2018 then bounes will not be declared"<<endl;

if(year>2018)
if (grade<16)
cout<<"if year is greater than 2018 then bounes will be declared "<<endl;
else
cout<<"if year is smaller than 2018 then bounes will not be declared"<<endl;



	return 0;
}
